spring:
  profiles: dev
  cloud:
    config:
      uri: http://localhost:9000
      name: application-okky, application-okky-member

---

spring:
  profiles: prod
  cloud:
    config:
      uri: http://config.okky.io:9000
      name: application-okky, application-okky-member